func climbStairs(n int) int {
  // create an array with the a[n] = number of steps
  a := []int{1,2}
  for i:=2; i<n; i++{
    n := a[i-1] + a[i-2]
    a = append(a,n)
  }
  // fmt.Println(a)
  return a[n-1]
}
