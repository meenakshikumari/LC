func subarraySum(nums []int, k int) int {
    var res, sum, i int
    h := make(map[int]int)
    for i<len(nums){
        sum += nums[i]
        if sum == k{
            res++
        }

        rem := sum - k
        res += h[rem]
        h[sum]++
        i++
    }
    return res
}
